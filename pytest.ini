[pytest]
# tests/ runs against the lightweight Home Assistant stub. tests_ha/ needs the
# real Home Assistant (requirements-ha-test.txt) and a separate process:
#     python -m pytest tests_ha
testpaths = tests
